import BookShelfLoading from '@/components/skelton/BookShelfLoading';

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <BookShelfLoading />;
}
